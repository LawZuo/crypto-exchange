server:
  port: 8078
spring:
  profiles:
    active: dev
    include:
      - feign
  application:
    name: service-mail

  mail:
    host: smtp.qq.com
    port: 587
    username: lawzhuo@qq.com
    password: bwezkusbvdzdcbcc  # QQ邮箱生成的【授权码】

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

bt:
  upstream:
    base-url: ${BT_UPSTREAM_BASE_URL:http://localhost:8078}