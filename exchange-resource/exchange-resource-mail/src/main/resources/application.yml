server:
  port: 8078
spring:
  profiles:
    active: dev
    include:
      - feign
      - mail
      - mq
  application:
    name: service-mail

bt:
  upstream:
    base-url: ${BT_UPSTREAM_BASE_URL:http://localhost:8078}
