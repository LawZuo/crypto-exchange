spring:
  data:
    redis:
      host: 106.12.183.54
      port: 63500
      password: LawZhuo1002.    # 无密码可留空
      database: 0                 # 使用的库索引
      timeout: 5000ms             # 命令超时时间
      lettuce:
        pool:
          max-active: 16          # 最大连接数
          max-idle: 8             # 最大空闲连接
          min-idle: 2             # 最小空闲连接
          max-wait: 3000ms        # 获取连接最大等待时间